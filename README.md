[project]
name = "openclaw_extension"
version = "0.1.0"
description = "Pre-scaffolded implementation of a user schema for OpenClaw integration"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]
