# codes/ — method files per format

Subfolders: file/, audio/, video/, image/, text/
Each file is a self-contained method. Never edit an existing file;
add a new one with a versioned suffix (.v2.md, .v3.md).
